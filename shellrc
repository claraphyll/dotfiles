if [ $TERM = "xterm-kitty" ]; then
  alias ssh="kitty +kitten ssh"
  alias rg="rg --hyperlink-format=kitty"
fi

alias key="nitropy nk3 secrets get-otp wa28ziqo-adm"

if [ -n "$ZSH_VERSION" ]; then
  setopt inc_append_history
fi

# vim: ft=sh
